@using Microsoft.EntityFrameworkCore.Metadata.Internal
@model ShopSite.Models.Category

@{
    ViewBag.Title = "GG";
}

<h3>Edit @Model.Name</h3>

<form class="form-horizontal" asp-action="Edit" method="post">
    <div class="form-group">
        <label class="col-sm-2 control-label">Category Name</label>
        <div class="col-sm-10">
            <input name="name" asp-for="Name" class="form-control"/>
        </div>
    </div>

    <div class="form-group">
        <label class="col-sm-2 control-label">Description</label>
        <div class="col-sm-10">
            <input name="description" asp-for="Description" rows="3" class="form-control"/>
        </div>

    </div>

    <div class="form-group">
        <label class="col-sm-2 control-label">Parent Category</label>
        <div class="col-sm-10">

            <input name="parentid" asp-for="ParentId" rows="3" class="form-control" />
            @*<select asp-for="ParentId" asp-items="@Model.p"></select>*@
            <span asp-validation-for="ParentId"></span>
        </div>
    </div>

    <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
                <button type="submit" value="Save" class="btn btn-primary"><span class="glyphicon glyphicon-ok"></span> Save</button>
            </div>
        </div>
</form>